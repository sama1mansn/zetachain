{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["error", "9.0.5"],
    "func-visibility": ["warn", { "ignoreConstructors": false }],
    "reason-string": ["warn", { "maxLength": 80 }],
    "no-empty-blocks": "off",
    "check-send-result": "off"
  }
}
